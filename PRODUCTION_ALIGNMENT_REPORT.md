# PRODUCTION ALIGNMENT REPORT

## Date: June 10, 2026
## Commit audited: 35534cb (main), ab45fe7 (staging-secure-office-foundation)
## Status: ❌ MISALIGNED — Production does NOT match repository main branch

---

## Finding

**Production (jamesroman.la) is NOT running `main`.** It is running the `staging-secure-office-foundation` branch — an entirely different application stack.

| Attribute | Production (jamesroman.la) | Main Branch (jr-advisory-test.vercel.app) |
|---|---|---|
| Branch | `staging-secure-office-foundation` | `main` |
| Auth provider | @clerk/nextjs ^7.4.2 | @supabase/ssr + @supabase/supabase-js |
| Database | @neondatabase/serverless | Supabase Postgres |
| Login route | `/sign-in` (Clerk hosted UI) | `/portal` (custom login form) |
| CSP | Clerk domains (crucial-chicken-28.clerk.accounts.dev) | Supabase + Stripe domains |
| Portal protection | Clerk middleware | Supabase cookie middleware |
| Admin workspace | Not present | 12-tab admin (7,698 lines) |
| E-signatures | Not present | Full lifecycle (request → sign → certificate) |
| Page builder | Not present | GrapesJS with 24 blocks |
| Tests | E2E (Playwright) | 199 unit tests (Vitest) |
| API endpoints | ~12 (estimated) | 34 |
| Database tables | Unknown (Neon) | 14 (Supabase, all with RLS) |

## Evidence

### Production headers (curl -sI https://www.jamesroman.la)
```
content-security-policy: ... https://crucial-chicken-28.clerk.accounts.dev ...
x-clerk-auth-status: signed-out
x-clerk-auth-reason: dev-browser-missing
```

### Production HTML
Contains Clerk JS bundle: `https://crucial-chicken-28.clerk.accounts.dev/npm/@clerk/clerk-js@6/dist/clerk.browser.js`

### Production /portal redirect
Redirects to `/sign-in?redirect_url=%2Fportal` (Clerk sign-in route)

### Test site headers (curl -sI https://jr-advisory-test.vercel.app)
```
content-security-policy: ... https://*.supabase.co ... https://js.stripe.com ...
```
No Clerk headers. Supabase + Stripe CSP. This matches `main`.

## Branch Divergence

- `staging-secure-office-foundation` is **22 commits ahead** of `main` (Clerk auth, Neon DB, visual prototype, GSAP animations, design fixes)
- `staging-secure-office-foundation` is **101 commits behind** `main` (all Phase 3-5 work, admin portal, signatures, messaging, page builder, security hardening, tests, database migrations)
- These branches share a common ancestor but have diverged completely in architecture

## Clerk commits on staging (not on main)
```
43c4258 feat: style Clerk sign-in/up pages to match site dark theme
bd7a86d fix: allow Clerk domains in CSP
445053c feat: Clerk auth on portal + consultation persistence
1c336ab feat: consultation persistence + Clerk auth on portal
```

## Risk Assessment

- **All security fixes, API hardening, and new features built since May are NOT live**
- The production site is running a prototype with Clerk auth in development mode (`dev-browser-missing` header)
- No unit tests exist on the staging branch
- The Neon database on staging may have different data/schema than the Supabase database on main

## Recommendation

**Switch production to `main`.** This requires:

1. Change Vercel production branch from `staging-secure-office-foundation` to `main`
2. Replace Clerk env vars with Supabase env vars on the production Vercel project
3. Remove Neon env vars, ensure Supabase env vars are set
4. Verify build succeeds and deploy is clean
5. Archive the `staging-secure-office-foundation` branch (already tagged as `prototype-live-2026-06-03`)

The prototype is preserved at tag `prototype-live-2026-06-03` and can be restored if needed.

---

**Report generated by Viktor · June 10, 2026**
